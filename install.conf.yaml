- defaults:
    link:
      relink: true

- clean: ['~']

- link:
  #    ~/.tmux.conf: tmux.conf
    ~/.config/nvim: nvim 

- create:
    - ~/.config/nvim 

- shell:
  - [git submodule update --init --recursive, Installing submodules]
